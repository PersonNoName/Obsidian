# 记忆系统完整指南

> 本文档整理自代码分析，描述 Mirror 项目的记忆系统架构、触发机制和数据流程。

---

## 一、记忆类型总览

Mirror 采用多层记忆架构，所有用户记忆最终汇聚到 **CoreMemory** 根容器中。

### 1.1 CoreMemory 顶级结构

```
CoreMemory (根容器)
├── self_cognition    # Agent 自我认知
├── world_model       # 用户/世界知识
├── personality       # 人格状态
└── task_experience   # 任务经验
```

**文件位置**: `app/memory/core_memory.py:282`

### 1.2 各类记忆详解

| 记忆类型 | 类名 | 所在位置 | 说明 |
|---------|------|---------|------|
| **自我认知** | `SelfCognition` | `:233` | Agent 理解自身能力/限制 |
| **世界模型** | `WorldModel` | `:244` | 用户事实、推断、关系 |
| **人格状态** | `PersonalityState` | `:259` | 性格、关系风格、会话适应 |
| **任务经验** | `TaskExperience` | `:273` | 压缩的任务学习摘要 |
| **确认事实** | `FactualMemory` | `:65` | 用户明确确认的知识 |
| **推断记忆** | `InferredMemory` | `:72` | AI 推断但未确认 |
| **关系记忆** | `RelationshipMemory` | `:79` | 主体-关系-客体 三元组 |
| **结构化教训** | `Lesson` | `app/tasks/models.py:45` | 从对话/任务提取的教训 |

### 1.3 SelfCognition 内部结构

```python
SelfCognition:
├── capability_map     # {domain: CapabilityEntry}  能力列表
├── known_limits       # [MemoryEntry]              已知限制
├── mission_clarity    # [MemoryEntry]              使命理解
├── blindspots         # [MemoryEntry]              盲点
└── version           # int                        版本号
```

### 1.4 WorldModel 内部结构

```python
WorldModel:
├── confirmed_facts        # [FactualMemory]      已确认事实
├── inferred_memories     # [InferredMemory]    推断记忆
├── relationship_history  # [RelationshipMemory] 关系历史
├── relationship_stage     # RelationshipStageState 关系阶段
├── proactivity_policy    # ProactivityPolicy   主动性策略
├── proactivity_state     # ProactivityState    主动性状态
├── pending_confirmations # [DurableMemory]     待确认记忆
└── memory_conflicts      # [DurableMemory]     记忆冲突
```

---

## 二、存储后端

| 后端 | 技术 | 用途 | 文件位置 |
|-----|------|-----|---------|
| **CoreMemoryStore** | PostgreSQL | CoreMemory 快照持久化 | `app/memory/core_memory_store.py` |
| **GraphStore** | Neo4j | 关系三元组存储 | `app/memory/graph_store.py` |
| **VectorRetriever** | Qdrant | 向量语义检索 | `app/memory/vector_retriever.py` |
| **SessionContextStore** | Redis | 会话消息和临时数据 | `app/memory/session_context.py` |
| **CoreMemoryCache** | 内存 + Redis | 用户级缓存 + 失效通知 | `app/memory/core_memory.py:291` |

### 2.1 存储关系图

```
┌─────────────────────────────────────────────────────────┐
│                    PostgreSQL                            │
│  ┌─────────────────┐  ┌─────────────────┐               │
│  │ core_memory_    │  │  tasks         │  ...          │
│  │ snapshots       │  │                │               │
│  └─────────────────┘  └─────────────────┘               │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│                   CoreMemoryCache                        │
│  (内存缓存 + Redis Pub/Sub 失效通知)                    │
└─────────────────────────────────────────────────────────┘
         │                    │                    │
         ▼                    ▼                    ▼
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│  Redis      │      │   Neo4j     │      │   Qdrant    │
│ (会话上下文) │      │  (关系图)   │      │  (向量检索) │
└─────────────┘      └─────────────┘      └─────────────┘
```

---

## 三、记忆触发机制

### 3.1 触发事件总表

| 事件类型 | 触发时机 | 处理器 |
|---------|---------|-------|
| `dialogue_ended` | 对话结束 | `SignalExtractor`, `ObserverEngine`, `ProactivityService` |
| `lesson_generated` | 生成教训 | `CognitionUpdater` |
| `task_completed` | 任务完成 | `MetaCognitionReflector` |
| `task_failed` | 任务失败 | `MetaCognitionReflector` |
| `hitl_feedback` | 人工确认反馈 | `CognitionUpdater`, `PersonalityEvolver` |

### 3.2 SignalExtractor - 对话信号提取

**文件**: `app/evolution/signal_extractor.py`

#### 会话级适应（不持久化）

| 信号类型 | 触发关键词 | 存储位置 |
|---------|-----------|---------|
| `prefer_concise` | "简洁一点", "简短", "concise", "shorter" | SessionAdaptation |
| `language_zh` | "中文", "说中文", "chinese" | SessionAdaptation |
| `tone_direct` | "少点客套", "直接一点" | SessionAdaptation |

#### 支持偏好（持久化）

触发关键词：
- **Listening 模式**: `"just listen"`, `"don't give advice"`, `"先听我说"`, `"不要急着给建议"`
- **Problem-solving 模式**: `"help me solve"`, `"tell me what to do"`, `"直接告诉我怎么做"`, `"帮我解决"`

生成 `Lesson` → 存入 `world_model.confirmed_facts` 或 `inferred_memories`

#### 主动性偏好（持久化）

触发关键词：
- **允许跟进**: `"check in later"`, `"follow up later"`, `"feel free to check in"`
- **禁止提醒**: `"don't remind me"`, `"don't follow up"`, `"no reminders"`

生成 `Lesson` → 存入 `world_model.proactivity_state.latest_preference_override`

### 3.3 ObserverEngine - 观察引擎

**文件**: `app/evolution/observer.py`

- **触发条件**: 30秒窗口 或 累积5条消息
- **提取方式**: LLM 提取三元组
- **关系类型**: `PREFERS`, `DISLIKES`, `USES`, `KNOWS`, `HAS_CONSTRAINT`, `IS_GOOD_AT`, `IS_WEAK_AT`
- **存储位置**: GraphStore (Neo4j) + VectorRetriever (Qdrant)

### 3.4 MetaCognitionReflector - 元认知反思

**文件**: `app/evolution/reflector.py`

- **触发条件**: `task_completed` 或 `task_failed` 事件
- **前提条件**: 任务存在于 task_store，且 lesson.confidence >= 0.5
- **LLM 提取字段**: `root_cause`, `lesson`, `is_agent_capability_issue`, `subject`, `relation`, `object`, `confidence`

### 3.5 CognitionUpdater - 认知更新器

**文件**: `app/evolution/cognition_updater.py`

处理 `lesson_generated` 事件，将 Lesson 分类并写入对应记忆：

```
lesson.is_agent_capability_issue = True
    └── 更新 SelfCognition.capability_map
    └── 增加/减少对应 domain 的 confidence

lesson.is_agent_capability_issue = False
    ├── domain = "support_preference"
    │       └── FactualMemory 或 InferredMemory
    ├── domain = "proactivity_preference"
    │       └── FactualMemory 或 InferredMemory
    ├── 有 subject + relation + object
    │       └── RelationshipMemory (存 Neo4j)
    └── 其他情况
            └── InferredMemory (默认)
```

---

## 四、记忆写入流程

```
┌──────────────────────────────────────────────────────────────┐
│  事件触发                                                     │
│  (dialogue_ended / task_completed / task_failed / etc.)      │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  SignalExtractor / ObserverEngine / MetaCognitionReflector   │
│  生成 Lesson 对象                                            │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  事件总线发布: lesson_generated                              │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  CognitionUpdater.handle_lesson_generated()                 │
│  - 分类 Lesson                                              │
│  - 治理策略检查 (is_blocked?)                                │
│  - 确认要求检查 (_requires_confirmation?)                    │
│    └── 是 → 创建 HITL 任务，等待用户确认                     │
│    └── 否 → 直接写入                                        │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  CoreMemoryScheduler.write()                                │
│  - 治理剪枝 (prune_and_record)                              │
│  - Token 预算限制 (共 5000 tokens)                          │
│    ├── self_cognition: 1000                                 │
│    ├── world_model: 1000                                    │
│    ├── personality: 800                                     │
│    └── task_experience: 1200                                │
│  - 保存 PostgreSQL 快照                                      │
│  - 更新内存缓存                                              │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  关系状态机更新 / 人格演化 (可选)                            │
└──────────────────────────────────────────────────────────────┘
```

---

## 五、记忆检索流程

```
┌──────────────────────────────────────────────────────────────┐
│  API 请求: POST /chat                                       │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  SoulEngine.run()  [app/soul/engine.py:110]                 │
└──────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ CoreMemoryCache │  │ SessionContext   │  │ VectorRetriever │
│     .get()      │  │ Store.get_      │  │   .retrieve()   │
│                 │  │ recent_messages │  │                 │
│ PostgreSQL ◄────┼──┤  (Redis)        │  │ Embedding ◄─────┼──► Qdrant
└─────────────────┘  └─────────────────┘  │                 │
                                           │ Optional ◄──────┴──► Reranker
                                           └─────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  _build_prompt() 格式化注入 System Prompt                   │
│                                                              │
│  ## Self Cognition                                           │
│  ## World Model                                              │
│  ## Stable Identity                                          │
│  ## Relationship Style                                       │
│  ## Relationship Stage                                       │
│  ## Proactivity Policy                                      │
│  ## Emotional Context                                        │
│  ## Support Policy                                           │
│  ## Session Adaptation                                       │
│  ## Task Experience                                          │
│  ## Available Tools                                          │
│  ## Constraints                                              │
│                                                              │
│  ## Session Raw Context (最近对话)                           │
│  ## Retrieved Context (向量检索结果)                          │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│  LLM 生成回复                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 六、Prompt 注入详情

**文件**: `app/soul/engine.py`

### 6.1 System Prompt 模板结构

```
SOUL_SYSTEM_PROMPT_TEMPLATE:
├── ## Self Cognition       ← SelfCognition 数据
├── ## World Model          ← WorldModel 数据
├── ## Stable Identity      ← PersonalityState.core_personality
├── ## Relationship Style   ← PersonalityState.relationship_style
├── ## Relationship Stage   ← WorldModel.relationship_stage
├── ## Proactivity Policy   ← WorldModel.proactivity_policy
├── ## Emotional Context    ← 情感分析结果
├── ## Support Policy       ← 支持模式决策
├── ## Session Adaptation   ← 当前会话适应（临时）
├── ## Task Experience      ← 任务经验摘要
├── ## Available Tools      ← 工具列表
├── ## Constraints          ← 行为约束
└── ## Output Format        ← XML 输出格式
```

### 6.2 World Model 格式化示例

```
## World Model

Confirmed Facts:
- [fact|active|confidence=0.95|source=dialogue_signal] User prefers listening-first support when emotionally loaded.

Inferred Memories:
- [inference|active|confidence=0.80|source=reflection] User often prefers problem-solving support in work contexts.

Relationship History:
- [relationship|active|confidence=0.75|source=observer] User KNOWS Python

Pending Confirmations:
- No pending confirmations.

Memory Conflicts:
- No memory conflicts.
```

---

## 七、当前限制与已知问题

### 7.1 为什么"我喜欢写代码"没有触发记忆？

**原因**: SignalExtractor 只识别预定义的关键词模式。

当前会触发记忆的关键词：
```
支持偏好: "just listen", "don't give advice", "先听我说", "不要急着给建议"
主动性:   "don't remind me", "check in later", "follow up later"
会话适应: "简洁一点", "说中文", "少点客套" (仅当前会话)
```

**通用事实**（如"我喜欢写代码"、"我在北京工作"）目前**不会被自动提取**。

### 7.2 ObserverEngine 的关系类型限制

当前只支持固定的关系类型：
- `PREFERS`, `DISLIKES`, `USES`, `KNOWS`
- `HAS_CONSTRAINT`, `IS_GOOD_AT`, `IS_WEAK_AT`

不支持任意关系的自由提取。

### 7.3 HITL 确认机制

以下情况需要人工确认：
- `sensitivity == "sensitive"`
- `confidence < 0.75`
- `confirmed_by_user == False`

---

## 八、文件索引

| 功能 | 文件路径 |
|-----|---------|
| 记忆模型定义 | `app/memory/core_memory.py` |
| 记忆存储 | `app/memory/core_memory_store.py` |
| 记忆缓存 | `app/memory/core_memory.py` (CoreMemoryCache 类) |
| 关系图存储 | `app/memory/graph_store.py` |
| 向量检索 | `app/memory/vector_retriever.py` |
| 会话上下文 | `app/memory/session_context.py` |
| 信号提取 | `app/evolution/signal_extractor.py` |
| 观察引擎 | `app/evolution/observer.py` |
| 元认知反思 | `app/evolution/reflector.py` |
| 认知更新 | `app/evolution/cognition_updater.py` |
| 记忆调度 | `app/evolution/core_memory_scheduler.py` |
| 情感引擎 | `app/soul/engine.py` |
| 事件总线 | `app/evolution/event_bus.py` |
| API 入口 | `app/api/chat.py` |
| 启动配置 | `app/runtime/bootstrap.py` |

---

## 九、待改进方向

1. **通用事实提取**: 扩展 ObserverEngine 支持任意关系类型的提取
2. **语义记忆**: 增加对用户兴趣、职业等通用背景信息的提取
3. **跨会话一致性**: 改进会话间记忆的有效性和利用率
4. **Prompt 自我认知**: 让 Agent 知道它有记忆系统能力，而非否认

---

*本文档由代码分析生成，如有疑问请检查对应源代码。*


